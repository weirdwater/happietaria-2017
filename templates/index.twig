{% extends "base.twig" %}

{% block content %}
  <section class="page-content__wrapper">
    <h2>{{foo}}</h2>
    <p>{{qux}}</p>
    {% for post in posts %}
      {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
    {% endfor %}
  </section>
{% endblock %}